# Python
__pycache__/
*.pyc
.venv/
.venv312/

# Secrets (2captcha / cfmail / mxroute keys)
.secret

# Runtime artifacts & output
results/
results_indo_open/
quiz_log*.txt
*.png
*.log
test_run_cookies.json

# Name pool state (cross-process no-repeat first names)
name_pool_state.json
name_pool_state.json.tmp
name_pool_state.lock

# Domain pool state (cross-process no-repeat cfmail domains)
domain_pool_state.json
domain_pool_state.json.tmp
domain_pool_state.lock

# Phone pool state (cross-process no-repeat phone numbers)
phone_pool_state.json
phone_pool_state.json.tmp
phone_pool_state.lock

# Agent/plugin local state and stray shell output
.omc/
nohup.out

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
